{
  "routes": [
    {
      "method": "GET",
      "path": "/members",
      "handler": "members.find",
      "config": {
        "policies": []
      }
    },
    {
      "method": "PUT",
      "path": "/members",
      "handler": "members.update",
      "config": {
        "policies": []
      }
    },
    {
      "method": "DELETE",
      "path": "/members",
      "handler": "members.delete",
      "config": {
        "policies": []
      }
    }
  ]
}
